PORT=4567
ENVIRONMENT=development
DATABASE_URL=sqlite://db/donation_platform.db
SESSION_SECRET=

# SMTP (generic)
SMTP_ADDRESS=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
MAIL_FROM=

# Brevo aliases (used if SMTP_* and MAIL_FROM are empty)
BREVO_SMTP_ADDRESS=
BREVO_SMTP_PORT=587
BREVO_SMTP_USERNAME=
BREVO_SMTP_PASSWORD=
BREVO_MAIL_FROM=
